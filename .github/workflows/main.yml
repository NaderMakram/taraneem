name: preRelease

on:
  release:
    types: [prereleased]

jobs:
  build-and-publish:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        with:
          clean: true

      - name: Use Node.js (adjust version if needed)
        uses: actions/setup-node@v3
        with:
          node-version: 20.x
          cache: "npm" # Cache npm dependencies

      - name: Install dependencies
        run: npm ci

      - name: Build the app for release
        run: npm run dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # Access token with release permissions

      - name: Wait for Build Completion
        run: sleep 10s

      - name: List Release Contents
        run: ls -R ./release/

      - name: Upload Release Artifacts
        uses: actions/upload-release-asset@v1
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: |
            ./release/${{github.ref_name}}/latest.yml
            ./release/${{github.ref_name}}/Taraneem-Windows*.exe
            ./release/${{github.ref_name}}/Taraneem-Windows*.exe.blockmap
          asset_name: |
            latest.yml
            Taraneem-Windows.exe
            Taraneem-Windows.exe.blockmap
          asset_content_type: application/octet-stream
      # - name: Publish the release
      #   uses: actions/create-release@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # Access token with release permissions
      #   with:
      #     tag_name: ${{ github.ref }}
      #     release_name: ${{ github.ref }} # Customize release name if desired
      #     draft: false # Publish the release after build and asset upload
